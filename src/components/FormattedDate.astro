---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString().trim()}
  >{
    date
      .toLocaleDateString("en-gb", {
        year: "numeric",
        month: "short",
        day: "numeric",
      })
      .trim()
  }</time
>
